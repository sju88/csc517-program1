<li>
      <%= gravatar_for student%>
      <%= link_to student.name, student %>
      | <%= link_to "histroy", studenthistroy_path(student_id: student.id)%>
      | <%= link_to "delete", student, method: :delete, data: { confirm: "You sure?" } %>
</li>